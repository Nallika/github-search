import styled from 'styled-components';

const Wrapper = styled.div`
  padding-top: 5px;
`;

const Link = styled.a`
  font-size: large;
  font-weight: bold;
  padding-top: 2px;
`;

const Text = styled.p`
  font-size: medium;
  padding: 0;
  margin: 0;
`

export { Wrapper, Link, Text }
